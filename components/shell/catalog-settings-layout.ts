/**
 * Shared layout tokens for the catalog settings modal.
 *
 * SettingsRow is a horizontal label/control pair in the BoardUI component.
 * On phone widths the pair stacks so long labels and wide controls (Google
 * sign-in, term picker) do not squeeze each other off-screen.
 */
export const SETTINGS_CARD_MOBILE =
  "[&>div]:max-sm:flex-col [&>div]:max-sm:items-start [&>div]:max-sm:gap-2";
